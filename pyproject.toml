[tool.poetry]
name = "library"
version = "0.1.0"
description = ""
authors = ["vyagofarov <yagofarov.vadim@mail.ru>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.13"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
